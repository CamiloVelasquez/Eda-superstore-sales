[project]
name = "trabajo-final"
version = "0.1.0"
description = "Trabajo final MLOps"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "numpy",
    "matplotlib",
    "seaborn",
    "jupyter"
]